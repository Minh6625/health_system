class AppBottomNavTokens {
  AppBottomNavTokens._();

  // Tab count
  static const int tabCount = 4;

  // Height base
  static const double heightBase = 72.0;

  // Icon size
  static const double iconSize = 24.0;

  // Touch target tối thiểu
  static const double minTouchHeight = 48.0;
  static const double minTouchWidth = 64.0;

  // Animation duration
  static const int animationDurationMs = 150;
}
