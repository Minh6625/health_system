import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Nền app
  static const Color bgPrimary = Color(0xFFF4F7FB);

  // Card chính
  static const Color bgSurface = Color(0xFFFFFFFF);

  // Hero / card nhấn nhẹ
  static const Color bgElevated = Color(0xFFEEF4FF);

  // Nội dung chính
  static const Color textPrimary = Color(0xFF12304A);

  // Phụ đề
  static const Color textSecondary = Color(0xFF5B7288);

  // Border nhẹ
  static const Color strokeSoft = Color(0xFFD8E3EE);

  // Active state, CTA phụ
  static const Color brandPrimary = Color(0xFF2F80ED);

  // Semantic Colors
  // Bình thường
  static const Color success = Color(0xFF2E9B6F);

  // Theo dõi
  static const Color warning = Color(0xFFF2A93B);

  // Nguy hiểm
  static const Color critical = Color(0xFFD95C5C);

  // SOS
  static const Color emergency = Color(0xFFC83D3D);

  // Device/offline/info
  static const Color info = Color(0xFF4B8BBE);
}

class AppStateColors {
  AppStateColors._();

  // Semantic backgrounds / tints
  static const Color successBg = Color(0xFFEAF5EF);
  static const Color warningBg = Color(0xFFFDF6E8);
  static const Color criticalBg = Color(0xFFFBEFEF);
  static const Color infoBg = Color(0xFFEDF3F8);
}
