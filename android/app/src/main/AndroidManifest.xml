<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!--
      BLE permissions for Phase 1 (real Redmi Watch 3 scan/connect).

      Android 12+ (API 31): BLUETOOTH_SCAN + BLUETOOTH_CONNECT are runtime
      permissions. We declare `neverForLocation` on SCAN so the OS does not
      force us to request ACCESS_FINE_LOCATION on Android 12+ purely for BLE.
      ACCESS_FINE_LOCATION is still kept above (already used elsewhere) and
      is required on Android 11 and below for BLE scanning.

      Android <= 11 (API 30): legacy BLUETOOTH + BLUETOOTH_ADMIN are still
      needed and we cap them with maxSdkVersion="30" so they are not granted
      unnecessarily on modern devices.
    -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />

    <!--
      Phase 2 (Health Connect): read-only access to Mi Fitness's
      Redmi Watch 3 readings. Each permission is a runtime grant on
      Android 14+; on older devices the Health Connect APK supplies
      the permission flow via its own activity.
    -->
    <uses-permission android:name="android.permission.health.READ_HEART_RATE" />
    <uses-permission android:name="android.permission.health.READ_STEPS" />
    <uses-permission android:name="android.permission.health.READ_SLEEP" />
    <uses-permission android:name="android.permission.health.READ_OXYGEN_SATURATION" />
    <uses-permission android:name="android.permission.health.READ_BODY_TEMPERATURE" />
    <uses-permission android:name="android.permission.health.READ_BLOOD_PRESSURE" />
    <uses-permission android:name="android.permission.health.READ_RESPIRATORY_RATE" />

    <!--
      Health Connect package visibility (Android 11+ package visibility).
      Without this <queries> entry, the `health` plugin cannot see
      `com.google.android.apps.healthdata` on Android 11+ and reports
      "Health Connect chua duoc cai dat" even when it is. The
      ACTION_SHOW_PERMISSIONS_RATIONALE intent is required by the
      package's permission flow on Android 14+.
    -->
    <queries>
        <package android:name="com.google.android.apps.healthdata" />
        <intent>
            <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
        </intent>
    </queries>

    <application
        android:label="Health Guard"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            
            <!-- Deep link configuration for email verification and password reset -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                
                <!-- Match standard HTTPS links from app.healthguard.com -->
                <data android:scheme="https" android:host="app.healthguard.com" android:pathPrefix="/verify-email" />
                <data android:scheme="http" android:host="192.168.1.101" android:pathPrefix="/verify-email" />
                <data android:scheme="http" android:host="localhost" android:pathPrefix="/verify-email" />
            </intent-filter>
            
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                
                <!-- Match standard HTTPS links from app.healthguard.com -->
                <data android:scheme="https" android:host="app.healthguard.com" android:pathPrefix="/reset-password" />
                <data android:scheme="http" android:host="192.168.1.101" android:pathPrefix="/reset-password" />
                <data android:scheme="http" android:host="localhost" android:pathPrefix="/reset-password" />
            </intent-filter>

            <!-- Specific intent filter for custom scheme (No autoVerify to prevent validation failures) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                
                <data android:scheme="healthguard" android:host="verify-email" />
                <data android:scheme="healthguard" android:host="reset-password" />
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Disable Impeller for Emulator Black Screen issue -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="google.navigation" />
        </intent>
    </queries>
</manifest>
